Routr is designed to help bike commuters find the best bike routes available to get to where they're going.

Ideas:
Implement Djikstra's algorithm with GIS data to find the shortest route between bike paths.

Future Ideas:
Identify areas of bike inefficiencies & contact representatives based on zip code!
Currently just want something to get you around.
Explore with weighting Djikstra's to prefer bike lanes, avoid busy roads, and no hills
Seasonality: Swap to paved paths in winter.

Preferences page:
What kind of paths you prefer, what you'd like to avoid, can you do hills, type of bike you have, etc.

Sightseeing option, find routes around a city (madison, boulder, etc) to see specific things you want!

9/8/2025:
- Got osmnx functioning, found cycling path data
- Now need to create class objects and get those set up

10/14/25:
- An AdjacencyView is a Read-only Map of Maps of Maps. 
    - It is a View into a dict-of-dict-of-dict data structure. The inner level of dict is read-write. But the outer levels are read-only.
    - Enter a node into the graph to see its AdjView, which contains all of the neighboring node. The desired neighboring node can then
        be provided to see the specific edge between the nodes & information like "length", "highway" & "osmid" can be accessed.

10/20/25:
- Shortest path function is now finally working!
- Next steps:
    - Create a class to have customization features:
        - When creating a bike class first the city is defined
        - Then there is the option to add attributes about routing preferences (bike path types)

    - Add in the priority queue to increase speed.  

10/23/25:
- Shortest path with a priority queue now working! Roughly 9 times faster than the original!
- Next steps:
    - Path distance 
    - Route decision options based on path type.
    - Set up classes & doc strings.
    - Visualizations of path (Start with geopandas explore?)